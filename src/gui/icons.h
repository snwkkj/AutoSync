#pragma once

#include <QIcon>
#include <QPixmap>

namespace autosync::icons {

QPixmap cpu();
QPixmap memory();
QPixmap temperature();
QIcon bug();
QIcon about();
QIcon synchronize();
QIcon unavailable();

} // namespace autosync::icons
